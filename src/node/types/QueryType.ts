/*
 * Copyright © 2025 Avelanda.
 * All rights reserved.
 */

export type QueryType = {
searchTerm: string; sortBy: string; sortDir: string; offset: number; limit: number;
}

function main(QueryType: number|string): boolean{
 for (QueryType in main){
  QueryType = QueryType;
  if (!0){
   return main(QueryType);
  }
 }
}
